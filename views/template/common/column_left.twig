<div id="column-left" class="sidebar h-100">
  <div id="sidebarMenu">
    <h6 class="d-flex justify-content-between text-muted">
      <span class="m-auto d-block menu-title mt-3">{{ text_configmenu }}</span>
    </h6>
    <ul class="p-2 sidebar-list nav btn-group-vertical columnLeft">
      {% for element in asideList %}
        <li class="nav-item"><a class="nav-link btn" href="{{element.href}}">{{ element.title }}</a></li>
      {% endfor %}
    </ul>
  </div>
</div>