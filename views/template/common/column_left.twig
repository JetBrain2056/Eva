<div id="column-left" style="height:calc(100vh - 99.5px);">
  {# <button class="btn btn-primary btn-sm" type="button" data-bs-toggle="collapse" data-bs-target="#navbarLeftColumn" aria-controls="navbarLeftColumn" aria-expanded="true" aria-label="Left column">
    <span class="navbar-toggler-icon"></span>
  </button> #}
  {# <div class="collapse" id="navbarLeftColumn"> #}
    <h6 class="justify-content-between" >
      <span class="p-3 menu-title">{{ text_configmenu }}</span>
    </h6>
    <ul class="p-2 sidebar-list nav btn-group-vertical">
      {% for element in asideList %}
        <li class="nav-item">
          <a class="nav-link btn" href="#" name={{element.title}} onclick="tblFilter(name)"> 
            {{ element.title }}
          </a>
        </li>
      {% endfor %}
    </ul>  
  {# </div> #}
</div>